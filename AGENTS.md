# 项目持久开发规则

本文件保存用户明确要求的开发原则，后续本项目所有改动均应遵守。

- 按 Android 正规开发规范实现，优先使用适合场景的原生/AndroidX 组件；保持现有 Kotlin、ViewBinding、轻量 MVVM 和单向状态流架构。
- 注重解耦与可维护性，按功能划分职责，用构造函数注入依赖；避免补丁式修改、巨型 Activity、无必要的架构层与通用基类。关键决策、生命周期和性能约束须有清晰注释。
- 每次实现都考虑 ANR、OOM、功耗与生命周期：耗时 I/O/计算离开主线程；限制并发与内存；协作式取消；及时关闭资源；页面不可见时停止无必要的观察、动画、采样与轮询。
- ViewModel/应用级对象不得持有 Activity、View、Binding 或大 Bitmap；文件明细分页/分批处理；不把全量数据放入 StateFlow、Bundle 或 Intent；不使用 GlobalScope、强制 GC、largeHeap 或后台保活规避设计问题。
- UI 使用 dp/sp、资源化文案和设计尺寸，处理系统栏/刘海 inset、大字体、小屏、横屏和触控可访问性。长列表使用可复用组件；避免多层滚动容器及无意义的持续重绘。
- 文本优先自然测量高度，不能直接将设计行高当成固定 View 高度；数字与单位通过 baseline 约束对齐。ConstraintLayout 不能替代字体度量检查，必须实际验证无裁切/遮挡，覆盖大字号与 OEM 字体差异。
- 按提供的 Figma 完整还原视觉与状态。图标和插画必须提取 Figma 原版资源并转换为 WebP，保留透明度、明确尺寸与来源；禁止手绘近似图标、Unicode/emoji 替代图标或用整页截图替代原生 UI。
- 图片转换在开发时完成；运行时使用合适密度和尺寸的本地资源，避免原图解码、大图读取、运行时转换和不必要的图片加载框架。
- 用户已授权 Network Traffic 和通知自动清理：通知清理仅适用于用户主动选中的应用，默认选择为空；通过系统通知监听服务按事件工作，不轮询、不申请保活豁免、不读取/存储通知正文。其余扫描/清理功能仍需另有任务授权。设计示例数据必须与真实数据源隔离，不伪装成实际结果。
- 修改后运行与改动相关的编译、Lint、测试和截图核验；实际性能结论须基于测量，不以架构设计或编译通过代替实测。未经要求不自动提交 Git 或推送。
- 用户已授权 Photo Compress、Large Files、Unused Files、Screenshots。共用扫描/分页索引/选择/确认/取消流程；Unused仅作为未修改时间的候选，不声称知道真实使用历史。压缩先创建并验证副本，删除仅针对用户选中并确认的文件，操作前重新验证访问权和文件状态。

- 用户已授权首页顶部 Smart Cleaning：支持垃圾候选分类与照片分析。相似/低清晰度仅作候选，不声称一定无用；按用户最新要求，新扫描完成后默认全选可清理候选，同一扫描内保留用户手动取消的选择。重复/相似组保护参考图，内容完全重复的文件删除前重新校验 SHA-256；不声称可读取或清理其他应用私有缓存。

- 用户已授权统一权限说明 UI 和授权后的自动返回：只在用户主动打开系统授权页后运行有期限的权限检测，授权/返回/取消/超时立即结束；后台返回仅用公开 API 尝试一次。不能绕过系统授权、后台启动限制或结束其他应用任务。普通运行时权限和目录选择以系统回调为准。

- 垃圾扫描当前只展示并清理 APK、Temporary Files、Empty Folders、Ad Files；旧分类与照片分析代码保留，但不进入当前入口的扫描、汇总和默认清理。临时后缀不设年龄门槛，cache 仅限可访问共享/授权目录；广告名称按词段及已知 SDK 特征识别。空目录递归确认无文件，每个 0B，删除前重新验证为空且只删除已选目录，禁止递归连带删除。扫描容量固定一位小数 MB，从 0.0 MB 开始，与进度条同步更新并收敛到实际结果。

- 用户于 2026-09-15 明确应用自身页面统一锁定竖屏；保留大字体、小屏和系统 inset 适配。

- 闲置文件按用户 2026-09-15 确认分为已安装 APK、已卸载应用在共享 Android/data 或 Android/obb 下的残留、Download 下超 30 天未修改的文档/压缩包。排除按年龄筛选的媒体及应用私有数据；无法确认包不存在或没有访问权时跳过，不猜测卸载状态。各功能口径独立，同一文件可在垃圾清理、大文件和闲置文件同时出现。删除前再次验证资格，残留只按选中快照逐项处理，目录为空才删除。
